{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.hyszh": {
			"states": {
				"default": {
					"transitions": [{
						"walking": "(1.0)"
					}]
				},
				"walking": {
					"transitions": [{
						"default": "query.get_equipped_item_name('main_hand')=='hys2' || query.get_equipped_item_name('main_hand')=='hys' && query.is_sneaking"
					}],
					"on_entry": ["/tag @s add hyszh"]
				}
			}
		}
	}
}